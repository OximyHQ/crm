# Communications own provider history

The CRM stores calls and messages as Communications with Participants and Artifacts. Activities project matched Communications onto record timelines. This keeps unmatched history complete and keeps Quo-specific delivery data outside the Activity model. The agent owns Quo access and identity matching. The API only verifies webhooks, stores deliveries, and queues agent work.
